# Latent Journey (CIMC Challenge Prototype)

A tiny interactive demo that lets a user traverse a semantic space by clicking “nearby meanings”.

## Run
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app/app.py